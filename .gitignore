# Build artifacts
/main
/bin/
*.exe

# Local runtime data — SQLite DB holds full indexed source + chat history
/data/
*.db
*.db-shm
*.db-wal

# Frontend
frontend/node_modules/
frontend/dist/

# LoRA training artifacts — large generated/binary output, not source.
# training/build_dataset.py, training/data/*.jsonl, and the Colab notebook
# are the actual source and stay tracked.
training/.venv/
training/adapters/
training/adapters_iter100/
training/fused-model/

# Editor/OS noise
.DS_Store
*.swp
